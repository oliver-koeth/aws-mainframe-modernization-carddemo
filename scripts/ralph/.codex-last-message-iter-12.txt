Implemented US-011 by adding shared write locking to `output/backend/app/storage.py`, documenting the locking strategy in `output/backend/README.md`, and extending `output/backend/tests/test_storage.py` with concurrency and timeout coverage. Backend checks passed with `.venv/bin/python -m pytest`, `.venv/bin/python -m mypy app`, and `.venv/bin/python -c "import app.main"`.
